# Environment / config for sample.http
# Use with: hello_client -c sample.env sample.http

timeout = 30
verbose = false
base_url = https://httpbin.org
param.token = my-secret-token
param.user_id = 42
